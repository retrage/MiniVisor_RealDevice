[package]
name = "mini_visor"
version = "1.0.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
log = "0.4.28"
uefi = { version = "0.36.0", features = [
    "alloc",
    "global_allocator",
    "logger",
    "panic_handler",
] }
